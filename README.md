
  # Clone existing design

  This is a code bundle for Clone existing design. The original project is available at https://www.figma.com/design/t2gYmA5acm20LumvMOip9H/Clone-existing-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  